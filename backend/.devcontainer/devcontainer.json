{
    "name": "tb-backend",
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".."
    },
    "workspaceFolder": "/app",
    "remoteUser": "root",
    "customizations": {
        "vscode": {
            "settings": {
                "pylint.args": [
                    "--load-plugins",
                    "pylint_django",
                    "--django-settings-module",
                    "config.settings.development"
                ]
            }
        }
    }
}
